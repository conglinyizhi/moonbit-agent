name = "conglinyizhi/coding_agent_other"

version = "0.1.0"

preferred_target = "native"

supported_targets = "native"

readme = "README.mbt.md"

repository = ""

license = "Apache-2.0"

keywords = [ "agent", "moonbit", "mcp" ]

description = "Minimal native MoonBit agent loop with TOML config and external MCP bridge."
